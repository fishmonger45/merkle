merkle

very simple merkle tree just to get my head around something. inputs block types are bytes which allows you to use whatever serialization of your choice without having to implement internal helper traits.
